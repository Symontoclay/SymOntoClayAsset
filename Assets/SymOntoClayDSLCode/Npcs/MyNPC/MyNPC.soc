app MyNPC
{
    on Init =>
    {
		"Begin" >> @>log;
        go();
	    "End" >> @>log;
    }
}

action Go
{
    op () => 
	{
	    @@host.`go`(to: #@[200]);
	}
	
//	on {: see(I, $x) & barrel($x) & distance(I, $x, $y) :} ($y >> @y, $x >> @x) => 
//	{
//	    "I see " >> @>log;
//		@x >> @>log;
//		@y >> @>log;
//		complete action;
//	}
	
	//on {: see(I, barrel) :} => {
	//    "It is barrel!" >> @>log;
		//@@host.`go`(to: #@[20, 180]);
		//complete action;
	//}
}